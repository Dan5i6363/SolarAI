# Demo guide

Run `scripts\run_demo.ps1`, select a scenario, advance replay, show trend charts, Architecture, then About the model. State that telemetry and metrics are synthetic and CV is heuristic.
