# Streamlit deployment

Push source, `models/`, `data/`, and `requirements.txt` to GitHub. In Streamlit Community Cloud set `dashboard/app.py` as the entry point. No secrets are required for demo mode. Cloud cannot use local OpenModelica, serial, webcam, or LoRa hardware.
