# Hardware integration

Set `SOLARAI_SERIAL_PORT` only after validated hardware is connected. Map sensors to canonical fields in `simulation/data_sources.py`. Validate SX1278 wiring, camera position, fault rules, and electrical safety physically; this project makes no hardware-performance claim.
