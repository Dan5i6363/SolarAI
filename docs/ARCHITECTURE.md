# Architecture

OpenModelica PV/MPPT (preserved), demo telemetry, or CSV replay provide canonical features. PyTorch classification plus heuristic CV feed the deterministic decision engine. JSON/CRC32 packets simulate the offline communication layer. Streamlit displays the result.
